# pip 21.2 (bundled with Python 3.9) cannot run an editable PEP 517 build in
# build isolation because its legacy "develop" callback re-invokes the target
# interpreter's pip module, which is shadowed by the isolation overlay.
# Installing without isolation uses the ambient setuptools and works on both
# legacy and modern pip.
[install]
no-build-isolation = true
